# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## AI 코딩 행동 원칙 (Karpathy Guidelines)

> **핵심 목적**: LLM의 흔한 코딩 실수를 줄이기 위한 행동 지침. 속도보다 정확성 우선.

### 1. 코딩 전 먼저 생각하기

**가정하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

구현 전에:
- 가정을 명시적으로 밝힌다. 불확실하면 먼저 질문한다.
- 해석이 여러 가지라면 제시한다 — 묵묵히 하나를 선택하지 않는다.
- 더 단순한 방법이 있다면 말한다. 필요할 때는 반론을 제기한다.
- 불명확한 점이 있으면 멈추고 이름을 붙여 질문한다.

### 2. 단순함 우선

**요청된 문제를 해결하는 최소한의 코드. 추측성 코드는 절대 금지.**

- 요청받은 것 이상의 기능은 추가하지 않는다.
- 단일 사용 코드에 추상화를 만들지 않는다.
- 요청되지 않은 "유연성"이나 "설정 가능성"을 추가하지 않는다.
- 불가능한 시나리오에 대한 에러 처리를 추가하지 않는다.
- 200줄로 쓴 것이 50줄로 가능하다면 다시 쓴다.

자문: "시니어 엔지니어가 이걸 보면 과복잡하다고 할까?" → 그렇다면 단순화.

### 3. 외과적 변경

**꼭 필요한 것만 건드려라. 내가 만든 쓰레기만 치워라.**

기존 코드 편집 시:
- 인접 코드, 주석, 포맷팅을 "개선"하지 않는다.
- 망가지지 않은 것은 리팩토링하지 않는다.
- 다르게 하고 싶어도 기존 스타일을 맞춘다.
- 관련 없는 데드코드를 발견하면 언급만 한다 — 삭제하지 않는다.

내 변경으로 고아가 된 것들:
- 내 변경으로 인해 사용되지 않게 된 import/변수/함수는 제거한다.
- 기존에 있던 데드코드는 요청받지 않는 한 제거하지 않는다.

기준: **모든 변경된 라인은 사용자의 요청으로 직접 추적 가능해야 한다.**

### 4. 목표 중심 실행

**성공 기준을 정의하라. 검증될 때까지 반복하라.**

작업을 검증 가능한 목표로 전환:
- "검증 추가" → "잘못된 입력에 대한 테스트 작성, 그 다음 통과시키기"
- "버그 수정" → "재현하는 테스트 작성, 그 다음 통과시키기"
- "X 리팩토링" → "전후로 테스트가 통과하는지 확인"

여러 단계 작업은 간단한 계획을 먼저 제시:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

---

## Git 워크플로우

@.claude/rules/GIT_GUIDELINES.md
